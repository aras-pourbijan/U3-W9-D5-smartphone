document.addEventListener("DOMContentLoaded", () => {
    let text = document.querySelector(".container p") as HTMLElement;
});

abstract class smartphone {
    marca: string;
    modello: string;
    anno: number;
    

    constructor(marca: string,
        modello: string,
        anno: number){
            this.marca=marca;
            this.modello=modello
            this.anno=anno
        }

        ricarica():void{

        }
        chiamnata():void{

        }
        public numero404()=>(x:number) {

        }
        ricarica():void{

        }
}
